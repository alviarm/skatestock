{
  "name": "skatestock-kafka-consumers",
  "version": "1.0.0",
  "description": "Kafka consumers for SkateStock data pipeline with idempotency and exactly-once processing",
  "main": "consumer.js",
  "scripts": {
    "start": "node consumer.js",
    "dev": "nodemon consumer.js",
    "test": "jest"
  },
  "dependencies": {
    "kafkajs": "^2.2.4",
    "pg": "^8.11.3",
    "ioredis": "^5.3.2",
    "winston": "^3.11.0",
    "uuid": "^9.0.0"
  },
  "devDependencies": {
    "nodemon": "^3.0.1",
    "jest": "^29.7.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
